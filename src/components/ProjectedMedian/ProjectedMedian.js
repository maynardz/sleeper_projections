import React, {useState, useEffect} from 'react';

const ProjectedMedian = (props) => {

  useEffect(() => {
    // props.teams.forEach((team) => {
    //   console.log(team);
    //   let starters = team.starters;
    //   console.log(starters);
    //   starters.map(async (s) => {
    //     console.log(s['proj'].stats.pts_ppr);
    //   })
    // })
  }, [props.teams]);

  return (
    <div>
      {/* proj */}
    </div>
  )
}

export default ProjectedMedian;